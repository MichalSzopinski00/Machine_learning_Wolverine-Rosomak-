from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='This is simple Machine learning project which will try to predict labels for the testing data, within this project we will try to use different ML models in order to determine which one will be suit better.',
    author='Jan / Michal',
    license='',
)
